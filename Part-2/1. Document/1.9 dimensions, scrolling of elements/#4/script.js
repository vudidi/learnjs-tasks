// В чём отличие CSS-свойств width и clientWidth

// В чём отличие между getComputedStyle(elem).width и elem.clientWidth?

// Укажите хотя бы 3 отличия, лучше – больше.

// Ответ:

// clientWidth возвращает размер с учетом полосы прокрутки, а getComputedStyle(elem).width может не вычитать
// полосу прокрутки при расчете ширины в некоторых браузерах

// clientWidth возвращает число, а getComputedStyle(elem).width строку с px.

// getComputedStyle возвращает "auto" для строчного элемента
