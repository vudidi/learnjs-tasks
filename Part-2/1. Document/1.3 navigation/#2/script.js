// Вопрос о соседях

// Если elem – произвольный узел DOM-элемента…

// Правда, что elem.lastChild.nextSibling всегда равен null? Да, т.к elem.lastChild - последний элемент, у которого нет соседнего элемента.
// Правда, что elem.children[0].previousSibling всегда равен null ? Нет, т.к. перед ним могут находиться другие узлы
